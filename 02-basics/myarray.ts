const superHeroes: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

type User = {
    name: string;
    isActive: boolean;
};

const allUsers: User[] = [];

const mlModels: number[][] = [
    [255, 255, 255],
    []
]

superHeroes.push("spiderman");
heroPower.push(34);

allUsers.push({name: "Daniel", isActive: true});